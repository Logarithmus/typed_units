mod prefix;
mod quantity;
mod unit;

pub use crate::{prefix::Prefix, quantity::Quantity, unit::root::Root};

fn main() {
    //let s1 = 23_i32 * km;
    //let s2 = 20_i32 * kilometer;
    //println!("{}", core::mem::size_of_val(&km));
    //dbg!(s1, s2);
}
